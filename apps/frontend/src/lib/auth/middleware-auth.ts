/** Auth cookie names for Edge middleware — keep in sync with @learnova/constants AUTH. */

export const MIDDLEWARE_AUTH = {
  REFRESH_COOKIE_NAME: 'learnova_session',
  ROLE_COOKIE_NAME: 'learnova_role',
} as const;
