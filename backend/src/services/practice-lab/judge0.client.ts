/**
 * @deprecated Import from `../coding-engine` instead.
 * Kept as a thin re-export so existing lab imports keep compiling.
 */
export {
  judge0Client,
  judge0IdForLanguage,
  mapJudge0StatusToExecutionStatus,
  type Judge0Result,
  type CreateSubmissionInput,
} from '../coding-engine/judge0.client.js';
